/**
 * 
 * @param {*} canvas -canvas节点
 * @param {*} widht -画布宽
 * @param {*} height -画布高
 * @param {*} options -配置项
 * @param {*} noScale -
 */

export default function(canvas,widht,height,options,noScale) {
    console.log(arguments,"这个是什么呀");
}